{
	"name": "@worldmaker/bitburner",
	"version": "1.0.0",
	"description": "Bitburner scripts",
	"main": "index.js",
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"bundle": "esbuild ./src/bootstrap-hack.ts --bundle --outfile=dist/service.js --target=es2018 --format=esm",
		"test": "echo \"Error: no test specified\" && exit 1",
		"prettier": "prettier --write .",
		"prettier:check": "prettier --check ."
	},
	"author": "",
	"license": "MIT",
	"devDependencies": {
		"esbuild": "^0.15.13",
		"prettier": "^2.7.1",
		"typescript": "^4.8.4"
	}
}
